import { FilerByNamePipe } from './filer-by-name.pipe';

describe('FilerByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilerByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
